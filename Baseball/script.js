;(function(){
    'use strict';

    const get = (element) => {
        return document.querySelector(element);
    }

    const $result = get('.ball_answer');
    const $input = get('ball_input');

    const init = () => {

    }

    init();
})()